import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  buttonSkill: {
    padding: 20,
    backgroundColor: '#1F1E25',
    borderRadius: 50,
    marginBottom: 8,
    alignItems: 'center',
  },
  textSkill: {
    color: '#FFF',
    fontSize: 22,
    fontWeight: 'bold',
  },
});
